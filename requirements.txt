# Requirements for DermalScan AI Streamlit app
# Note: the module imported as `cv2` is provided by the
# `opencv-python` package (use `pip install opencv-python`).
streamlit==1.51.0
Pillow==12.0.0
numpy==2.2.6
pandas==2.3.2
matplotlib==3.10.7
opencv-python==4.12.0.88
tensorflow==2.20.0
h5py==3.15.1
requests==2.32.5
pyarrow==21.0.0
